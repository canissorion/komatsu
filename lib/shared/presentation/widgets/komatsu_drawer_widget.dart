import 'package:flutter/material.dart';

class KomatsuDrawerWidget extends StatelessWidget {
  const KomatsuDrawerWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
